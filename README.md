# AdaptRoute
AdaptRoute is a task-aware routing framework that turns a single small language model into a multi-domain expert using dynamic LoRA adapter switching. It classifies queries (code, math, QA, etc.) and loads the optimal adapter in real time, enabling efficient, low-latency performance on a single GPU.
